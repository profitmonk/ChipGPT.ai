// Reuse the Open Graph card for the Twitter/X card too, so twitter:image is wired
// to the same generated "Silicon Bug Files" route.
export { default, runtime, alt, size, contentType } from "./opengraph-image";
